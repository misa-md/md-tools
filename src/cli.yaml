name: md-tools
version: "0.1.0"
author: genshen. <genshenchu@gmail.com>
about: Crystal MD conversion tools, compatible with Crystal MD v0.3.x.
args:
  - format:
      short: f
      long: format
      required: true
      value_name:  FORMAT
      help: output format(xyz, text, db, def)
      takes_value: true
  - ranks:
      short: r
      long: ranks
      required: true
      value_name: RANKS
      help: ranks to run the parallel program
      takes_value: true
  - input:
      short: i
      long: input
      required: true
      help: Sets the filename of input file
      takes_value: true
  - output:
      short: o
      long: output
      help: Sets the filename of output file
      default_value: md-output
      takes_value: true
